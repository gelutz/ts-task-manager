import { extendTheme } from "@chakra-ui/react";

export const globalTheme = extendTheme({
    styles: {
        global: {
            
        },
        fonts: {
            heading: 'JetBrains',
            body: 'JetBrains'
        }
    }
})